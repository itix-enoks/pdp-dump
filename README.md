# Fetch generated binaries on ce-portal by copying and pushing it this github repo. 
After running `copy-bin.pl`, u can pull the new binaries on your local machine and flash FPGA.

-- Look at `copy-bin.pl` for more details
